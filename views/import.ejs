<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Import Data - ZTube</title>
  <script src="https://cdn.tailwindcss.com/3.4.3"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
</head>

<body class="bg-zinc-900 text-zinc-100">
  <div class="flex h-screen">
    <!-- Sidebar -->
    <%- include('partials/sidebar', { activePage: 'import' }) %>

      <!-- Main Content Area -->
      <div id="main-content" class="flex-1 flex flex-col ml-16">
        <nav class="bg-green-600 text-white p-2 sticky top-0 z-50 w-full">
          <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold text-zinc-800">ZTube</h1>
            <%- include('partials/navbarSearch') %>
          </div>
        </nav>

        <main class="container mx-auto p-4 flex-grow">
          <div id="loadingIndicator" class="text-center py-4 hidden"><i
              class="fas fa-spinner fa-spin mr-2"></i>Loading...
          </div>
          <div id="errorDisplay" class="text-red-500 text-center py-2 hidden"></div>

          <!-- Page Header -->
          <div class="mb-8">
            <h2 class="text-3xl font-bold text-zinc-100 mb-2">Import Data from FreeTube</h2>
            <p class="text-zinc-400 text-lg">Import your subscriptions, playlists, and watch history from FreeTube to
              continue where you left off.</p>
          </div>

          <!-- Import Sections Grid -->
          <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">

            <!-- Subscriptions Import -->
            <div class="bg-zinc-800 p-6 rounded-lg shadow-lg">
              <div class="flex items-center mb-4">
                <i class="fas fa-users text-2xl text-blue-500 mr-3"></i>
                <h3 class="text-xl font-semibold text-zinc-100">Subscriptions</h3>
              </div>
              <p class="text-zinc-400 text-sm mb-4">
                Import your FreeTube subscriptions from a CSV export file.
              </p>
              <div class="space-y-3">
                <div class="flex items-center space-x-3">
                  <input type="file" id="subscriptionsFileInput" accept=".csv" class="hidden">
                  <button id="selectSubscriptionsFileBtn"
                    class="bg-zinc-700 hover:bg-zinc-600 text-white font-bold py-2 px-4 rounded transition duration-150 flex-1">
                    <i class="fas fa-file-csv mr-1"></i> Select CSV File
                  </button>
                </div>
                <div id="selectedSubscriptionsFile" class="text-zinc-400 text-sm min-h-[1.25rem]"></div>
                <button id="uploadSubscriptionsBtn" disabled
                  class="w-full bg-blue-600 hover:bg-blue-700 disabled:bg-zinc-600 disabled:cursor-not-allowed text-white font-bold py-2 px-4 rounded transition duration-150">
                  <i class="fas fa-cloud-upload-alt mr-1"></i> Import Subscriptions
                </button>
                <div id="subscriptionsImportProgress" class="hidden">
                  <div class="bg-zinc-700 rounded-full h-2 mb-2">
                    <div id="subscriptionsProgressBar" class="bg-blue-600 h-2 rounded-full transition-all duration-300"
                      style="width: 0%"></div>
                  </div>
                  <p id="subscriptionsProgressText" class="text-zinc-400 text-sm">Uploading...</p>
                </div>
              </div>
            </div>

            <!-- Playlists Import -->
            <div class="bg-zinc-800 p-6 rounded-lg shadow-lg">
              <div class="flex items-center mb-4">
                <i class="fas fa-list-ul text-2xl text-green-500 mr-3"></i>
                <h3 class="text-xl font-semibold text-zinc-100">Playlists</h3>
              </div>
              <p class="text-zinc-400 text-sm mb-4">
                Import your FreeTube playlists from a database export file.
              </p>
              <div class="space-y-3">
                <div class="flex items-center space-x-3">
                  <input type="file" id="playlistsFileInput" accept=".db,.ndjson" class="hidden">
                  <button id="selectPlaylistsFileBtn"
                    class="bg-zinc-700 hover:bg-zinc-600 text-white font-bold py-2 px-4 rounded transition duration-150 flex-1">
                    <i class="fas fa-database mr-1"></i> Select DB File
                  </button>
                </div>
                <div id="selectedPlaylistsFile" class="text-zinc-400 text-sm min-h-[1.25rem]"></div>
                <button id="uploadPlaylistsBtn" disabled
                  class="w-full bg-green-600 hover:bg-green-700 disabled:bg-zinc-600 disabled:cursor-not-allowed text-white font-bold py-2 px-4 rounded transition duration-150">
                  <i class="fas fa-cloud-upload-alt mr-1"></i> Import Playlists
                </button>
                <div id="playlistsImportProgress" class="hidden">
                  <div class="bg-zinc-700 rounded-full h-2 mb-2">
                    <div id="playlistsProgressBar" class="bg-green-600 h-2 rounded-full transition-all duration-300"
                      style="width: 0%"></div>
                  </div>
                  <p id="playlistsProgressText" class="text-zinc-400 text-sm">Uploading...</p>
                </div>
              </div>
            </div>

            <!-- Watch History Import -->
            <div class="bg-zinc-800 p-6 rounded-lg shadow-lg">
              <div class="flex items-center mb-4">
                <i class="fas fa-history text-2xl text-purple-500 mr-3"></i>
                <h3 class="text-xl font-semibold text-zinc-100">Watch History</h3>
              </div>
              <p class="text-zinc-400 text-sm mb-4">
                Import your FreeTube watch history from a database export file.
              </p>
              <div class="space-y-3">
                <div class="flex items-center space-x-3">
                  <input type="file" id="historyFileInput" accept=".db,.ndjson" class="hidden">
                  <button id="selectHistoryFileBtn"
                    class="bg-zinc-700 hover:bg-zinc-600 text-white font-bold py-2 px-4 rounded transition duration-150 flex-1">
                    <i class="fas fa-database mr-1"></i> Select DB File
                  </button>
                </div>
                <div id="selectedHistoryFile" class="text-zinc-400 text-sm min-h-[1.25rem]"></div>
                <button id="uploadHistoryBtn" disabled
                  class="w-full bg-purple-600 hover:bg-purple-700 disabled:bg-zinc-600 disabled:cursor-not-allowed text-white font-bold py-2 px-4 rounded transition duration-150">
                  <i class="fas fa-cloud-upload-alt mr-1"></i> Import History
                </button>
                <div id="historyImportProgress" class="hidden">
                  <div class="bg-zinc-700 rounded-full h-2 mb-2">
                    <div id="historyProgressBar" class="bg-purple-600 h-2 rounded-full transition-all duration-300"
                      style="width: 0%"></div>
                  </div>
                  <p id="historyProgressText" class="text-zinc-400 text-sm">Uploading...</p>
                </div>
              </div>
            </div>

          </div>

          <!-- Instructions Section -->
          <div class="mt-8 bg-zinc-800 p-6 rounded-lg shadow-lg">
            <h3 class="text-xl font-semibold text-zinc-100 mb-4">
              <i class="fas fa-info-circle text-blue-500 mr-2"></i>
              How to Export from FreeTube
            </h3>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
              <div>
                <h4 class="font-semibold text-zinc-200 mb-2">Subscriptions</h4>
                <ol class="text-zinc-400 text-sm space-y-1">
                  <li>1. Open FreeTube</li>
                  <li>2. Go to Settings → Data Settings</li>
                  <li>3. Click "Export Subscriptions"</li>
                  <li>4. Choose CSV format</li>
                  <li>5. Save the file and upload it here</li>
                </ol>
              </div>
              <div>
                <h4 class="font-semibold text-zinc-200 mb-2">Playlists</h4>
                <ol class="text-zinc-400 text-sm space-y-1">
                  <li>1. Open FreeTube</li>
                  <li>2. Go to Settings → Data Settings</li>
                  <li>3. Click "Export Playlists"</li>
                  <li>4. Save the database file</li>
                  <li>5. Upload the file here</li>
                </ol>
              </div>
              <div>
                <h4 class="font-semibold text-zinc-200 mb-2">Watch History</h4>
                <ol class="text-zinc-400 text-sm space-y-1">
                  <li>1. Open FreeTube</li>
                  <li>2. Go to Settings → Data Settings</li>
                  <li>3. Click "Export History"</li>
                  <li>4. Save the database file</li>
                  <li>5. Upload the file here</li>
                </ol>
              </div>
            </div>
          </div>

        </main>
      </div>

      <script type="module" src="/js/import.js"></script>
</body>

</html>