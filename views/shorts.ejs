<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shorts - ZTube</title>
  <script src="https://cdn.tailwindcss.com/3.4.3"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
</head>

<body class="bg-zinc-900 text-zinc-100">
  <div class="flex h-screen">
    <!-- Sidebar -->
    <%- include('partials/sidebar', { activePage: 'shorts' }) %>

      <!-- Main Content Area -->
      <div id="main-content" class="flex-1 flex flex-col ml-16">
        <nav class="bg-green-600 text-white p-2 sticky top-0 z-50 w-full">
          <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold text-zinc-800">ZTube Shorts</h1>
            <%- include('partials/navbarSearch') %>
          </div>
        </nav>

        <main class="container mx-auto p-4 flex-grow">
          <!-- Shorts Filter/Category Tabs -->
          <div class="flex space-x-4 mb-6">
            <button id="trendingShortsBtn"
              class="px-4 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors">
              Trending Shorts
            </button>
            <button id="subscriptionShortsBtn"
              class="px-4 py-2 bg-zinc-700 text-zinc-200 rounded-lg hover:bg-zinc-600 transition-colors">
              Subscription Shorts
            </button>
          </div>

          <!-- Loading indicator -->
          <div id="shortsLoadingIndicator" class="hidden flex justify-center items-center py-10">
            <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-green-500"></div>
            <span class="ml-2 text-zinc-400">Loading Shorts...</span>
          </div>

          <!-- Shorts Content Grid -->
          <div id="shortsContent" class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6 gap-4">
            <!-- Shorts will be dynamically loaded here -->
          </div>

          <!-- Error message -->
          <div id="shortsErrorMessage" class="hidden text-center text-red-400 py-10">
            <i class="fas fa-exclamation-triangle text-2xl mb-2"></i>
            <p>Failed to load Shorts. Please try again later.</p>
          </div>

          <!-- Empty state -->
          <div id="shortsEmptyState" class="hidden text-center text-zinc-400 py-20">
            <i class="fas fa-play text-6xl mb-4 opacity-50"></i>
            <h3 class="text-xl mb-2">No Shorts Found</h3>
            <p>Try checking trending Shorts or make sure you have subscriptions.</p>
          </div>
        </main>

        <%- include('partials/videoPlayer') %>

      </div>
  </div>

  <script src="https://www.youtube.com/iframe_api"></script>
  <script src="/js/shorts.js" type="module"></script>
</body>

</html>